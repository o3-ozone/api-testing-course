info:
  name: Order without Token
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/orders"
  body:
    type: json
    data: '{ "itemId": 1, "quantity": 1 }'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("สั่งอาหารโดยไม่แนบ Token ต้องถูกบล็อกด้วย Status 401", function() {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
