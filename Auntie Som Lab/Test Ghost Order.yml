info:
  name: Test Ghost Order
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/orders/ORD-FAKE999"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("หาออเดอร์ไม่เจอต้องคืนค่า 404 (ทดสอบบั๊ก Status Code ผิด)", function() {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
